<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>DB Admin - <%= table %></title>
    <link rel="stylesheet" href="/admin-style.css">
</head>
<body>
  <div class="header-admin">BrandShop — Администрирование</div>
  <div class="admin-container">
    <div class="admin-title">Таблица: <%= table %></div>
    <a class="admin-link" href="/admin">← К списку таблиц</a>
    <a class="admin-btn" href="/admin/table/<%= table %>/add">➕ Добавить запись</a>
    <div class="admin-table-wrapper">
      <table class="admin-table">
        <thead>
            <tr>
                <% columns.forEach(col => { %>
                    <th><%= col.name %></th>
                <% }) %>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% rows.forEach(row => { %>
                <tr>
                    <% columns.forEach(col => { %>
                        <td><%= row[col.name] %></td>
                    <% }) %>
                    <td>
                        <a class="admin-btn" href="/admin/table/<%= table %>/edit/<%= row.id %>">✏️</a>
                        <form method="POST" action="/admin/table/<%= table %>/delete/<%= row.id %>" style="display:inline;">
                            <button class="admin-btn" type="submit" onclick="return confirm('Удалить запись?')">🗑️</button>
                        </form>
                    </td>
                </tr>
            <% }) %>
        </tbody>
      </table>
    </div>
  </div>
</body>
</html>
